/**
 * Placeholder CI scripts until feat/scaffolding adds ESLint, Prettier, Vitest, Vite.
 * Exits 0 so GitHub Actions pipeline can run end-to-end.
 */
const step = process.argv[2] ?? 'unknown';
console.log(`[ci-stub] ${step}: ok (replace when app tooling lands)`);
